{
  "name": "command-deck",
  "private": true,
  "version": "1.0.0",
  "description": "Animated portfolio tracker across all projects — static site + a Vercel serverless function backed by Postgres (Neon).",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
